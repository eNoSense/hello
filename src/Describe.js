import React from 'react';

export default function Describe () {
	return (
		<div className="desc">
			<p>任务一：使用 React Router</p>
			<p>请用 create-react-app 创建一个应用，满足以下要求</p>

			<p>用户访问 localhost:8080/1，页面中会出现 1</p>
			<p>用户访问 localhost:8080/2，页面中会出现 2</p>
			<p>代码可以在 codesandbox.io 中写，也可以本地用然后上传到 GitHub</p>
		</div>
	)
}
